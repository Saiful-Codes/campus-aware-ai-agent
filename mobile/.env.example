# Mobile environment variables
# Copy this file to mobile/.env and replace with your laptop's IPv4 address.
# Do NOT use "localhost" — Expo Go on a physical device cannot reach the laptop's localhost.
# All frontend-exposed env vars must be prefixed with EXPO_PUBLIC_.

EXPO_PUBLIC_API_BASE_URL=http://YOUR_LAPTOP_IPV4:8000
